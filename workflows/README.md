# Workflows

Markdown SOPs (Standard Operating Procedures) for the WAT framework.

Each workflow defines:

- **Objective** -- what the workflow accomplishes
- **Required inputs** -- what data or context is needed
- **Tools** -- which scripts from `tools/` to execute
- **Expected outputs** -- what the workflow produces
- **Edge cases** -- how to handle failures or unexpected situations
